{% if options.menu_title or options.menu_title_image %}
    <div id="responsive-menu-title">
        {% if options.menu_title_link %}
            <a href="{{ options.menu_title_link }}" target="{{ options.menu_title_link_location }}">
        {% endif %}

        {% if options.menu_title_image %}
            <div id="responsive-menu-title-image">{{ options.menu_title_image }}</div>
        {% endif %}

        {% if options.menu_title_link %}
            </a>
        {% endif %}

        {% if options.menu_title_link %}
            <a href="{{ options.menu_title_link }}" target="{{ options.menu_title_link_location }}">
        {% endif %}

        {{ options.menu_title }}

        {% if options.menu_title_link %}
            </a>
        {% endif %}
    </div>
{% endif %}