<div class="panel panel-default">
    <div class="panel-body">
        These are the options you probably want to set first to get you started.
        You can view the docs for this section by clicking <a href="https://responsive.menu/docs/initial-setup/" target="_blank">here.</a>
    </div>
    <table class="table table-bordered table-hover">
        <tr{% if errors.breakpoint %} class="danger"{% endif %}>
            <td class="well col-md-3">
                <label for="menu_title" class="control-label">
                    Breakpoint
                    <div class="sub-text">This is the width of the screen at which point you would like the menu to start showing in pixels.</div>
                    <div class="example">Set to 80000 if you want the menu to appear on all devices.</div>
                </label>
            </td>
            <td>
                <input type="text" name="menu[breakpoint]" value="{{ options.breakpoint }}" class="form-control" id="breakpoint" placeholder="800">
                <span class="unit">px</span>
            </td>
        </tr>
        <tr{% if errors.menu_to_use %} class="danger"{% endif %}>
            <td class="well col-md-3">
                <label for="menu_title" class="control-label">
                    Menu to Use
                    <div class="sub-text">If no options appear here, make sure you have set them up under <strong>Appearance > Menus</strong> or click <a href="{{ admin_url }}nav-menus.php">here</a>.</div>
                </label>
            </td>
            <td>
                <select class="selectpicker show-tick" name="menu[menu_to_use]">
                    {% for value, display in nav_menus %}
                        <option value="{{ value }}" {% if options.menu_to_use == value %}selected="selected"{% endif %}>
                            {{ display }}
                        </option>
                    {% endfor %}
                </select>
            </td>
        </tr>
        <tr{% if errors.menu_to_hide %} class="danger"{% endif %}>
            <td class="well col-md-3">
                <label for="menu_to_hide" class="control-label">
                    CSS Selector of Menu to Hide
                    <div class="sub-text">Any legal CSS selction criteria is valid here.</div>
                    <div class="example">e.g: #nav-menu, nav, .other-nav</div>
                </label>
            </td>
            <td>
                <input type="text" name="menu[menu_to_hide]" value="{{ options.menu_to_hide }}" class="form-control" id="menu_to_hide" placeholder="#nav-menu, nav">
            </td>
        </tr>
    </table>
</div>
{% for option in options %}
 - {{ option }}
{% endfor %}